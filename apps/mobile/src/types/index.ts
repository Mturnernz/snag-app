// Types moved to packages/shared-types so apps/web can share them — see
// SNAG_WEB_APP_PLAN.md §1. Re-exported here so every existing relative
// `from '../types'` import in this app keeps working unchanged.
export * from '@snag/shared-types';
