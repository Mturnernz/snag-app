export default function CheckEmailPage() {
  return (
    <section className="container" style={{ padding: '64px 32px', maxWidth: 440, textAlign: 'center' }}>
      <h1 style={{ marginBottom: 12 }}>Check your email</h1>
      <p style={{ color: 'var(--color-text-secondary)' }}>
        We've sent you a confirmation link. Once you confirm and log in, your organisation will be
        set up automatically — no extra steps.
      </p>
    </section>
  );
}
